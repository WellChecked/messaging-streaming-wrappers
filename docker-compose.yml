services:

  mosquitto:
    image: eclipse-mosquitto
    container_name: msw-mqtt-broker
    ports:
      - 1883:1883
      - 9001:9001
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    networks:
      - msw_net

networks:
  msw_net:
    driver: bridge

